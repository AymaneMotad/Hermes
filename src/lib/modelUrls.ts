/**
 * Feature flag and asset URLs for 3D scene.
 * Set USE_READY_AGENTS to true when you have a GLB character model to use.
 */
export const USE_READY_AGENTS = false;

/** URL to GLB character model (used when USE_READY_AGENTS is true). */
export const CHARACTER_GLB_URL = '/models/character.glb';

/** Scale applied to the GLB when rendering. */
export const CHARACTER_SCALE = 1;
